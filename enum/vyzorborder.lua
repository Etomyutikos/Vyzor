local Ennum = require("vyzor.enum")

local _enum = {
    Top = "Top",
    Bottom = "Bottom",
    Right = "Right",
    Left = "Left"
}

local VyzorBorder = Enum("VyzorBorder", _enum)

return VyzorBorder